#include <stdio.h>
void merge(int* arr,int* temp,int left,int mid,int right){
    //合并arr[left]~arr[mid]与arr[mid+1]~arr[right]
    //1.将arr的内容备份到temp里面

    //i访问左半边，j访问右半边，k存放结果
}

int main() {
    printf("Hello, World!\n");
    return 0;
}
